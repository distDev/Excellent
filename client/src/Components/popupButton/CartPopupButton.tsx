import React from 'react'
import { StyledPopupButton } from '../StyledComponents/StyledPopupButton';

type Props = {}

const CartPopupButton = (props: Props) => {
  return (
   <StyledPopupButton bg='dark'>

  </StyledPopupButton>
  )
}

export default CartPopupButton